$(function(){$('input[type="date"]').on("change",function(){const $startDate=$("#startDate"),$baseDate=$("#baseDate"),$endDate=$("#endDate"),startDate=$startDate.val(),baseDate=$baseDate.val(),endDate=$endDate.val();$("#resultDiv").empty();if(validate(startDate,baseDate,endDate)){let rate=calculateRate(startDate,baseDate,endDate);$("#resultDiv").append(rate+"%")}})});function validate(startDate,baseDate,endDate){let isValid=false;if(startDate==""||startDate.length<1){return isValid}if(baseDate==""||baseDate.length<1){return isValid}if(endDate==""||endDate.length<1){return isValid}if(!(startDate<=baseDate&&startDate<=endDate)){return isValid}if(!(baseDate<=endDate)){return isValid}isValid=true;return isValid}function calculateRate(startDate,baseDate,endDate){const totalCount=(new Date(endDate)-new Date(startDate))/864e5+1;const progressCount=(new Date(baseDate)-new Date(startDate))/864e5+1;const rate=Math.floor(progressCount/totalCount*100);return rate}